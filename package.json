{
    "name": "quiz-multiplayer",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "start": "node server/index.js",
      "heroku-postbuild": "cd client && npm install && npm run build"
    }
  }
  